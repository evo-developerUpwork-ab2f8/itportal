<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Software hardwareItem</title>
    <style>
      div {
        margin: 100px auto;
      }
      ul li {
        list-style: none;
        color: blue;
      }
      span {
        font-weight: bolder;
        color: black;
      }
    </style>
  </head>
  <body>
    <div>
      <ul>
        <% suppliers.forEach(function(val){ %>
          <li>
            <a href='/supplier/<%=val._id%>'><%= val.name %></a>
          </li><hr />
        <%})%>
      </ul>
    </div>
  </body>
</html>
